<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jitc.adminjitc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminJITC">
        <activity android:name=".UI.GetStartedActivity"></activity>
        <activity android:name=".UI.MenuActivity" />
        <activity android:name=".pendaftaran.ListPendaftarActivity" />
        <activity android:name=".uploadcourse.DetailCourseActivity" />
        <activity android:name=".uploadcourse.MainActivity" />
        <activity android:name=".video.ViewVideoActivity" />
        <activity android:name=".video.UploadVideoActivity" />
        <activity android:name=".uploadcourse.ViewCourseActivity" />
        <activity android:name=".UI.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>